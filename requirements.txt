# BioMark — Python Dependencies
# Install with: pip install -r requirements.txt
# Python 3.8+ required

# ── Core system & hardware detection ──────────────────────
psutil>=5.9.0
py-cpuinfo>=9.0.0

# ── Numerical computing ───────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0

# ── Report generation ─────────────────────────────────────
# (HTML report uses only stdlib + numpy; no extra deps needed)

# ── Development & testing (optional) ─────────────────────
# pytest>=7.4.0
# pytest-cov>=4.1.0
